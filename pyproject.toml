[project]
name = "hoopla"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "argparse>=1.4.0",
    "nltk==3.9.1",
    "numpy<2",
    "sentence-transformers>=5.1.1",
    "torch==2.2.2",
]
